# Ably SDK Test Credentials Template
# 
# IMPORTANT: Integration-First Testing Requirements
# - Copy this file to ably-credentials.env
# - Fill with REAL Ably sandbox credentials (NO MOCKS!)
# - Never commit ably-credentials.env to git
# - Use a dedicated test app in Ably dashboard
#
# To get credentials:
# 1. Log into Ably dashboard: https://ably.com/dashboard
# 2. Create a new app for testing
# 3. Use the sandbox environment
# 4. Copy the API key from the app dashboard

# Sandbox API Key (format: appId.keyId:secret)
ABLY_API_KEY_SANDBOX=BGkZHw.WUtzEQ:wpBCK6EsoasbyGyFNefocFYi7ESjkFlyZ8Yh-sh0PIA

# App ID (extracted from API key)
ABLY_APP_ID_SANDBOX=BGkZHw

# Environment (always 'sandbox' for testing)
ABLY_ENVIRONMENT=sandbox

# Optional: Test namespace prefix (for test isolation)
ABLY_TEST_NAMESPACE_PREFIX=rust_sdk_test_

# Optional: Test data TTL in seconds (for automatic cleanup)
ABLY_TEST_DATA_TTL=300

# Rate limiting configuration
ABLY_TEST_RATE_LIMIT_PER_SECOND=10
ABLY_TEST_RATE_LIMIT_BURST=20